import React from 'react'

const SubheaderShopB = (props) => {
    return (
        <div>
          <div>
            <h2 className='subheader-shop'>{props.title}</h2>
          </div>
        </div>
    )
}

export default SubheaderShopB
